events {
    worker_connections 1024;
}

http {
    include       mime.types;
    default_type  application/octet-stream;
    sendfile        on;
    keepalive_timeout  65;

    # Map for WebSocket Upgrade header
    map $http_upgrade $connection_upgrade {
        default upgrade;
        ''      close;
    }

    upstream atm_backend {
        # 'backend' refers to the service name in docker-compose
        server backend:4000;
    }

    # HTTP Server: Redirect to HTTPS
    server {
        listen 80;
        server_name _;
        
        # Let's Encrypt validation
        location /.well-known/acme-challenge/ {
            root /var/www/certbot;
        }

        location / {
            return 301 https://$host$request_uri;
        }
    }

    # HTTPS Server
    server {
        listen 443 ssl http2;
        server_name _;

        # SSL Certificates (mounted via Docker volume from certbot/let's encrypt)
        ssl_certificate /etc/letsencrypt/live/serviceapotheke.tech/fullchain.pem;
        ssl_certificate_key /etc/letsencrypt/live/serviceapotheke.tech/privkey.pem;

        # SSL Config (Mozilla Intermediate)
        ssl_protocols TLSv1.2 TLSv1.3;
        ssl_ciphers ECDHE-ECDSA-AES128-GCM-SHA256:ECDHE-RSA-AES128-GCM-SHA256:ECDHE-ECDSA-AES256-GCM-SHA384:ECDHE-RSA-AES256-GCM-SHA384:ECDHE-ECDSA-CHACHA20-POLY1305:ECDHE-RSA-CHACHA20-POLY1305:DHE-RSA-AES128-GCM-SHA256:DHE-RSA-AES256-GCM-SHA384;
        ssl_prefer_server_ciphers off;
        ssl_session_timeout 1d;
        ssl_session_cache shared:MozSSL:10m;
        ssl_session_tickets off;

        # Static assets routing
        location /assets/ {
            proxy_pass http://atm_backend/assets/;
            expires max;
            add_header Cache-Control "public, max-age=31536000, immutable";
        }

        # API & App routing
        location / {
            proxy_pass http://atm_backend;
            
            # Standard Proxy Headers
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;

            # WebSocket Support (Required for Supabase Realtime & Jitsi WebRTC Signaling)
            proxy_http_version 1.1;
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection $connection_upgrade;
            
            # Increased timeouts for long-running WebRTC/Realtime connections
            proxy_read_timeout 3600s;
            proxy_send_timeout 3600s;
        }
    }
}
